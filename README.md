# Multi-Processor-CPU-Scheduling-Simulator
Multi-processor CPU scheduling simulator written in C++ using POSIX threads (pthreads). It supports multiple scheduling algorithms and demonstrates process execution across multiple processors.
